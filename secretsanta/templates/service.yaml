apiVersion: v1
kind: Service
metadata:
  name: {{ include "secretsanta.fullname" . }}
  labels:
    {{- include "secretsanta.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "secretsanta.selectorLabels" . | nindent 4 }}
